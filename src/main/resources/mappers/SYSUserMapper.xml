<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huangzi.frame.mapper.SYSUserMapper">

    <!--获取数据列表-->
    <select id="list" resultType="SYSUser">
        select
          sysUser.id,
          sysUser.user_account,
          sysUser.user_pass,
          sysUser.delete_flag,
          sysUser.created_time,
          sysUser.update_time
        from sys_user sysUser
        where sysUser.delete_flag = 0

        <if test=" condition.userAccount != null and condition.userAccount != '' ">
            and sysUser.user_account like '%${ condition.userAccount }%'
        </if>

    </select>

    <!--获取数据总数-->
    <select id="total" resultType="Integer">
        select
          count(*)
        from sys_user sysUser
        where sysUser.delete_flag = 0
    </select>

</mapper>
